# Enable CPU Hardware Virtualization Passthrough (AMD-V)
vhv.enable = "TRUE"

# Force HW-assisted MMU / Nested Paging (RVI)
nestedHV = "TRUE"

# Enforce hardware-assisted virtualization and MMU execution mode
monitor.virtual_exec = "hardware"
monitor.virtual_mmu = "hardware"
